//
//  main.swift
//  Implementation_LibraryFine
//
//  Created by Samuel A WINFUL on 2/14/16.
//  Copyright © 2016 Samuel A WINFUL. All rights reserved.
//

var fine = 0

struct Date {
  var day   = 1
  var month = 1
  var year  = 2000
  
  func readDate() {
    let dateLine = readLine()!.charactersSeperatedBy(" ").
  }
  
  private func setDate(d: Int, m: Int, y: Int) {
    day   = d
    month = m
    year  = y
  }
}

if bookReturned <= returnDate {
  // No fine charged. Fine is zero (0)
} else if bookReturned.month == returnDate.month {
  // fine = 15 Hackos x number of days late.
} else if bookReturned.month != returnDate.month {
  if bookReturned.year == returnedDate.year {
    // fine = 500 Hackos x number of late months.
  } else if bookReturned.year != returnDate.year {
    // fine = 10,000 Hackos
}
